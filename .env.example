# Copy this file to `.env` and paste your key. `.env` is gitignored, so it is
# never committed. rustio-draft reads the key locally and sends it only to
# Anthropic's API.
ANTHROPIC_API_KEY=sk-ant-your-key-here
